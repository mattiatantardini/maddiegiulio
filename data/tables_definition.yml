tables:
  [
    {
      tablename: names_new,
      schema: public,
      drop_if_exists: False,
      primary_key: {cols: ["UID"]},
      columns:
        {
          UID: SERIAL,
          NAME: VARCHAR(200),
          MESSAGE: TEXT,
          DATE_SAVED: TIMESTAMP,
        }
    },

  ]
